send_rdv_multiple:
  build:
    files: ./send_rdv_multiple.c
    sources:
      binary: send_rdv_multiple
      cflags: -g -O0
  run:
    program: "@SRCPATH@/script.sh @BUILDPATH@/send_rdv_multiple"
send_rdv_sym:
  build:
    files: ./send_rdv_sym.c
    sources:
      binary: send_rdv_sym
      cflags: -g -O0
  run:
    program: "@SRCPATH@/script.sh @BUILDPATH@/send_rdv_sym"
send_rdv_unsym:
  build:
    files: ./send_rdv_unsym.c
    sources:
      binary: send_rdv_unsym
      cflags: -g -O0
  run:
    program: "@SRCPATH@/script.sh @BUILDPATH@/send_rdv_unsym"
partitioned:
  build:
    files: ./partitioned.c
    sources:
      binary: partitioned
      cflags: -g -O0
  run:
    program: "@SRCPATH@/script.sh @BUILDPATH@/partitioned"
